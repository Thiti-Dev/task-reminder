export enum Action{
    ADD_TASK = 'Add Task',
    REMOVE_TASK = 'Remove task',
    DONE_TASK = 'Mark task as done',
    LIST_TASK = 'See what tasks are going on',
    LIST_FINISHED_TASK = 'See what tasks I have done already',
    EXIT = 'Exit'
}